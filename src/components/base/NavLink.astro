---
export interface Props {
  href: string;
  linkText: string;
}

const { href, linkText } = Astro.props;
---

<li><a href={href} class="hover:text-slate-600" data-testid="navLink">{linkText}</a></li>

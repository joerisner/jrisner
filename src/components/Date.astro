---
export interface Props {
  date: string;
  testId?: string;
}

const { date, testId = 'publishDate', ...rest } = Astro.props;
---

<time class="text-sm text-muted md:text-base" datetime={date} data-testid={testId} {...rest}>{date}</time>

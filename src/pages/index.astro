---
import { SITE_TITLE } from '../config';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title={SITE_TITLE}>
  <section class="flex-grow-2 my-auto">
    <h1 class="font-mono mb-5 pl-1 text-accent-purple text-xl">Hi, my name is</h1>
    <h2 class="mb-5 text-7xl">Joseph Risner.</h2>
    <div class="inline-block">
      <h2 class="mb-5 text-7xl text-slate-500 border-r-2 border-slate-500 typewriter">I build things for the web.</h2>
    </div>
    <p class="mb-5 text-slate-500 w-3/5">
      I'm a software engineer specializing in testing (and occasionally building) web applications. Currently, I'm
      focused on building an E2E test suite at <a
        href="https://aumni.fund"
        class="text-accent-purple"
        data-testid="employerLink">Aumni</a
      >.
    </p>
    <a
      href="/blog"
      class="inline-block hover:bg-violet-900 hover:outline-none hover:text-primary mt-3 outline outline-1 px-5 py-3 rounded text-accent-purple"
      data-testid="blogLink">Check out the latest blog articles</a
    >
  </section>
  <div class="h-20"></div>
</BaseLayout>

<style>
  .typewriter {
    animation: typing 3s steps(20, end) forwards, blink 800ms steps(20) infinite;
    line-height: 1.1;
    overflow: hidden;
    white-space: nowrap;
    width: 0;
  }

  @keyframes typing {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }

  @keyframes blink {
    from {
      border-color: transparent;
    }
    to {
      @apply border-slate-500;
    }
  }
</style>
